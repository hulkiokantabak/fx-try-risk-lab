# Legacy Archive

This folder holds the older server-first workstation and earlier design material.

It remains in the repository for reference, but it is no longer the main way to use FX TRY Risk Lab.

Normal use should start from:

- `docs/` for the public browser app
- `scripts/build_browser_data.py` for rebuilding published snapshot data
- `start-browser.ps1` for local preview
